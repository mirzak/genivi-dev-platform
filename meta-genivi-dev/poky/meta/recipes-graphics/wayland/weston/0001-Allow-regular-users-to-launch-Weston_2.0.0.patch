Index: weston-2.0.0/libweston/launcher-direct.c
===================================================================
--- weston-2.0.0.orig/libweston/launcher-direct.c
+++ weston-2.0.0/libweston/launcher-direct.c
@@ -274,9 +274,10 @@ launcher_direct_connect(struct weston_la
 			int tty, const char *seat_id, bool sync_drm)
 {
 	struct launcher_direct *launcher;
-
+#ifndef ENABLE_SYS_UID
 	if (geteuid() != 0)
 		return -EINVAL;
+#endif
 
 	launcher = zalloc(sizeof(*launcher));
 	if (launcher == NULL)
Index: weston-2.0.0/configure.ac
===================================================================
--- weston-2.0.0.orig/configure.ac
+++ weston-2.0.0/configure.ac
@@ -155,6 +155,12 @@ if test x$enable_xkbcommon = xyes; then
                           true)
 fi
 
+AC_ARG_ENABLE(sys-uid, [  --enable-sys-uid],,
+              enable_sys_uid=no)
+if test x$enable_sys_uid = xyes; then
+   AC_DEFINE(ENABLE_SYS_UID, [1], [Allow regular users to launch Weston])
+fi
+
 AC_ARG_ENABLE(setuid-install, [  --enable-setuid-install],,
 	      enable_setuid_install=yes)
 AM_CONDITIONAL(ENABLE_SETUID_INSTALL, test x$enable_setuid_install = xyes)
